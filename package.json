{
  "name": "devops.knowit.no-ghpages",
  "version": "1.0.0",
  "description": "devops.knowit.no",
  "scripts": {
    "build": "jade --out . --obj src/viewmodel.json --pretty src/index.jade",
    "start": "jade --out . --obj src/viewmodel.json --pretty --watch src/index.jade"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/knowit/devops.knowit.no.git"
  },
  "homepage": "http://devops.knowit.no/",
  "devDependencies": {
    "jade": "^1.11.0"
  }
}
